# AI Features Guide

## Обзор

Приложение теперь включает 10 мощных AI-фич, работающих полностью офлайн:

## 1. 🗣️ NLP Parser (Парсер естественного языка)

**Компонент**: `NLPTaskInput`
**Сервис**: `NLPParserService`

Преобразует свободный текст в структурированные задачи.

### Примеры:

- "Купить молоко завтра утром" → задача на завтра 9:00
- "Позвонить врачу в пятницу в 14:00" → задача на пятницу 14:00
- "Оплатить счёт каждый месяц" → повторяющаяся задача
- "Тренировка каждый понедельник" → еженедельная задача

### Возможности:

- Извлечение даты/времени (абсолютное и относительное)
- Определение повторяемости (daily/weekly/monthly)
- Автоматическая категоризация
- Расчёт приоритета по ключевым словам
- Извлечение тегов (#хэштеги)

## 2. ⚡ Priority Scoring (Приоритизация)

**Сервис**: `PriorityService`

Умный расчёт приоритета задач на основе:

- Давление дедлайна (42%)
- Ключевые слова (25%)
- Категория (18%)
- Стрик пользователя (10%)
- Длительность задачи (5%)

### Использование:

```typescript
const score = PriorityService.calculateScore(task);
const topTasks = PriorityService.getTopTasksForToday(tasks, 3);
```

## 3. 🔄 Pattern Detection (Обнаружение паттернов)

**Сервис**: `PatternDetectionService`

### Цепочки задач:

Находит задачи, которые часто выполняются вместе.
Пример: "Gym" → "Shake" → "Pack bag"

### Повторяющиеся привычки:

Замечает, когда вы обычно создаёте задачи и напоминает.

Пример: Если каждый понедельник в 17:00 создаёте "Weekly report", система предложит это.

## 4. 🎯 Adaptive RT Scheduling (Адаптивное расписание)

**Сервис**: `NotificationRTService`

Выбирает оптимальное время для уведомлений на основе:

- Вероятности открытия в течение 5 минут (Beta-распределение)
- Медианного времени реакции (log-normal)
- Усталости от уведомлений
- Приоритета задачи

### Алгоритм:

```
attention = 0.5*pOpen5m + 0.3*pOpen30m + 0.2*rtFactor
score = priority * attention * (1 - fatigue) * deadlineFactor
```

## 5. ⏰ Personalized Snooze (Персональный Snooze)

**Сервис**: `PersonalizedSnoozeService`

Предлагает умные интервалы откладывания:

1. Медианное время реакции для этой категории/времени
2. 2x медианное время
3. Следующее окно высокой вероятности действия

### Использование:

```typescript
const options = PersonalizedSnoozeService.getSuggestions(taskId, category);
// Возвращает: [30, 60, 120] (минуты)
```

## 6. 🌈 Enhanced Day Vibe (Улучшенный виб дня)

**Сервис**: `EnhancedDayVibeService`

Анализирует характер дня с z-оценкой нагрузки:

- **Busy** (z ≥ 1.0): Перегруженный день
- **Focus** (z ≥ 0.5): День концентрации
- **Balanced** (|z| < 0.5): Сбалансированный
- **Chill** (z ≤ -1.0): Спокойный день
- **Free**: Свободный день

### Расчёт:

```
load = Σ(weight(category) * count)
z = (load - expectedLoad) / σ
```

## 7. 🔍 Search & Deduplication (Поиск и дедупликация)

**Сервис**: `SearchAndDedupeService`

### Поиск:

- Токенизация и триграммы
- Fuzzy matching
- Фильтры по категории, приоритету, дате
- Подсветка совпадений

### Дедупликация:

- Jaccard similarity ≥ 0.9
- Проверка категории и даты
- Автослияние при высокой похожести

## 8. 🏆 Achievement System (Система достижений)

**Сервис**: `AchievementService`

Мотивация через:

- Стрики открытия (3/7/14/30 дней)
- Стрики категорий
- Гибкие цели (например, "5 задач до 10:00")

## 9. 🤖 AI Coordinator (Координатор AI)

**Сервис**: `AICoordinatorService`

Центральный хаб, объединяющий все AI-фичи:

### Методы:

```typescript
// Обработка естественного языка
const suggestion = await AICoordinatorService.processNaturalLanguage(
  text,
  tasks,
);

// Получение дашборда
const dashboard = await AICoordinatorService.getDashboard(tasks);

// Обработка завершения задачи
const result = await AICoordinatorService.onTaskCompleted(task, allTasks);

// Периодический анализ
await AICoordinatorService.runPeriodicAnalysis(tasks);
```

## 10. 🎨 AI Dashboard (AI Дашборд)

**Компонент**: `AIDashboard`

Визуализация всех AI-инсайтов:

- Виб дня с градиентом
- Топ-3 приоритета
- Напоминания о привычках
- Предложения цепочек
- Дубликаты
- Достижения

## Использование в приложении

### 1. Подключение хука:

```typescript
import { useAI } from '../hooks/useAI';

const {
  dashboard,
  parseNaturalLanguage,
  getSnoozeOptions,
  onTaskCompleted,
  searchTasks,
} = useAI(tasks);
```

### 2. Создание задачи с NLP:

```typescript
<NLPTaskInput tasks={tasks} onCreateTask={handleCreate} onClose={handleClose} />
```

### 3. Отображение дашборда:

```typescript
<AIDashboard
  tasks={tasks}
  onTaskPress={handleTaskPress}
  onCreateTask={handleCreate}
  onMergeTasks={handleMerge}
/>
```

## Хранение данных

Все данные хранятся локально:

- **MMKV**: Быстрое key-value хранилище для метрик
- **Realm**: Основная база данных задач
- **SQLite**: Индексы для поиска (опционально)

### Ключи MMKV:

- `rt_slot_stats::{category}:{dow}:{bin}` - RT статистика
- `snooze-history` - История snooze
- `task-chains` - Цепочки задач
- `recurring-patterns` - Повторяющиеся паттерны
- `load-history` - История нагрузки
- `search-index` - Поисковый индекс

## Производительность

Все алгоритмы оптимизированы для работы на устройстве:

- NLP парсинг: <10ms
- Priority scoring: <5ms
- Pattern detection: <100ms (фоновая задача)
- Search: <50ms
- Day vibe: <20ms

## Конфиденциальность

✅ Все данные остаются на устройстве
✅ Нет отправки в облако
✅ Нет зависимости от интернета
✅ Полный контроль пользователя
